# dotfiles

Personal system-setup/dotfiles repo, one folder per machine/OS.

## Machines

| Folder | Machine | OS |
|---|---|---|
| [`cachyos/`](cachyos/) | ThinkPad T14 Gen2 | CachyOS (Arch-based) |

More machines (Ubuntu/Debian, macOS) to be added as their own top-level folders, each
with its own `install.sh`/README following the same pattern as `cachyos/`.

## Other

- [`ubuntu-vs-arch-cli.md`](ubuntu-vs-arch-cli.md) - general Ubuntu vs Arch/CachyOS CLI
  command reference, not specific to any one machine.
